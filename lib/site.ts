// Single source of truth for site-wide contact details. Change it here once and
// every page (about, legal, support, footer, transactional email) follows.
// Client-safe (plain constants, no env reads) so display stays identical
// everywhere. Set up `care@pravara.com` as an alias that forwards to your inbox.
export const CONTACT_EMAIL = "care@pravara.ai";
